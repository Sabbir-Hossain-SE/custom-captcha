const INITIAL_TOLERANCE = 0.9; // 90% accuracy required initially
const TOLERANCE_DECREASE = 0.1; // Decrease by 10% per attempt
const MIN_TOLERANCE = 0.5; // Minimum 50% accuracy required

export { INITIAL_TOLERANCE, TOLERANCE_DECREASE, MIN_TOLERANCE };
